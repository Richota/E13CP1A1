<h1>Ingresar una venta</h1>
<%= form_with(model: @sale, local: true) do |f| %>

  <table class="table">
    <tbody>
      <tr>
        <th scope="row"><%= f.label :cod, "Código" %></th>
        <td><%= f.number_field :cod %> </td>
      </tr>
      <tr>
        <th scope="row"><%= f.label :detail, "Detalle" %></th>
        <td><%= f.text_area :detail, rows: 4 %></td>
      </tr>
      <tr>
        <th scope="row"><%= f.label :category, "Categoria" %></th>
        <td><%= f.text_field :category%></td>
      </tr>
      <tr>
        <th scope="row"><%= f.label :value, "Valor" %></th>
        <td><%= f.number_field :value %></td>
      </tr>
      <tr>
        <th scope="row"><%= f.label :discount, "Descuento" %></th>
        <td><%= f.number_field :discount %></td>
      </tr>
      <tr>
        <th scope="row"><%= f.label :tax, "IVA" %></th>
        <td><%= f.check_box :tax %></td>
      </tr>
      <tr>
        <th scope="row"><%= f.label "TOTAL" %></th>
        <td><%= %></td>
      </tr>
    </tbody>
  </table>

    <p></p>
    <%= f.submit "Ingresar", class: 'btn btn-info' %>
<% end %>
